const gulp = require('gulp')
const styles = require('./gulp/tasks/sass')
const helpers = require('./gulp/tasks/helpers')
const bootstrap = require('./gulp/tasks/bootstrap')
const bootstraph = require('./gulp/tasks/bootstraph')
const jquery = require('./gulp/tasks/jquery')
const bscripts = require('./gulp/tasks/bscripts')
module.exports.sass = gulp.series(styles)
module.exports.helpers = gulp.series(helpers)
module.exports.bootstrap = gulp.series(bootstrap)
module.exports.bootstraph=gulp.series(bootstraph)  
module.exports.jquery=gulp.series(jquery)
module.exports.bscripts=gulp.series(bscripts)      
module.exports.deploy=gulp.series(styles, bootstrap, jquery, bscripts, bootstraph, helpers)
